import { HighliteMouseDirective } from './highlite-mouse.directive';

describe('HighliteMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HighliteMouseDirective();
    expect(directive).toBeTruthy();
  });
});
